server:
  port: 8084

spring:
  application:
    name: payment-service

  datasource:
    url: jdbc:h2:mem:paymentDb;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    driverClassName: org.h2.Driver
    username: nik
    password: nik
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  h2:
    console:
      enabled: true
      path: /h2-payment